#include <stdio.h>

// This is a comment, ignored by the compiler.

// This is the main function.
int main() {
	// Statements here are executed when the compiled binary is called.
	
	// Print text to the console.
	
	printf("Hello World!\n");
	printf("I'm a C Programmer!\n");

	return 0;
}

// In C, the `printf` function is used to print text to the console, and the `return 0;` 
// statement indicates a successful program execution. Note that C does not have built-in
// support for macros like Rust, so the `!` in the `println!` macro is not present in the
// C code.
